{
  "name": "hangar-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test scripts/check-delta-headings.test.mjs && node scripts/check-delta-headings.mjs && pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "openspec:validate": "node scripts/validate-openspec-cn.mjs"
  }
}
