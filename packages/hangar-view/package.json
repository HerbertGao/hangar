{
  "name": "@hangar/view",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "hangar-view — 脊柱外、单用户、只读的运行呈现层(零 import @hangar/core;只经 hangar CLI --json + 只读 app.yaml)",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "node-cron": "^4.6.0",
    "yaml": "^2.9.0"
  }
}
